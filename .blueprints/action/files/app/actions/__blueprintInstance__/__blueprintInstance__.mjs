import path from 'path'
import fs from 'fs-extra'

import Blueprint from '../../lib/Blueprint.mjs'

import {
  PROJECT_BLUEPRINTS_PATH,
  GLOBAL_BLUEPRINTS_PATH,
} from '../../config.mjs'

export default async function {{blueprintInstance}}(options) {
  try {
    // action steps
  } catch (err) {
    console.error(err)
  }
}